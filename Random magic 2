#include<stdio.h>
int main()
{
    int magic,guess;
    magic=rand();
    scanf("%d",&guess);
    if(guess==magic)
        printf("found");
    else
    {
        printf("Wrong\n");
        if(guess>magic)
            printf("too high");
        else
            printf("too low");
    }
}
